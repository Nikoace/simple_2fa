# Simple 2FA — 前端 & Tauri 容器

本目录包含 React 前端代码和 Tauri 桌面应用容器。

详细文档请参阅根目录 [README.md](../README.md)。

## 常用命令

```bash
# 开发模式
bun run tauri dev

# 构建
bun run tauri build

# Rust 测试
cd src-tauri && cargo test

# Rust 格式化 + Lint
cd src-tauri && cargo fmt && cargo clippy
```
